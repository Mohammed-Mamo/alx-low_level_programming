#include "main.h"
#include <stdio.h>
/**
 * main - function
 * @argc: counter
 * @argv: vector
 * Return: integer
 */
int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[0]);

	return (0);
}
