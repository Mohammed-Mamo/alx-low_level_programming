#include "main.h"
#include <stdio.h>
/**
 * main - check main fun
 * @argc: counter
 * @argv: vector
 * Return:0 succes
 */
int main(int argc, char *argv[])
{
	if (argc > 0)
		printf("%d\n", argc);
	return (0);
}
