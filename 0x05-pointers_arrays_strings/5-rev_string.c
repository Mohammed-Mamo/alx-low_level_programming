#include "main.h"
/**
 * rev_string - function that swaps string
 * @*s: is an input 
 *
 * return: nothing
 */
void rev_string(char *s)
{

	char temp;
	int l = _strlen(*s);
	
	while (i < len)
	{
temp = s[i];
s[i] = s[len];
s[len] = temp;
i += 1;
len -= 1;
	}
}
